#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <time.h>

void BubbleSort(int* array, int length)
{

}

int main()
{
	srand(time(NULL));
	// random a number between 0 and 100
	int randNumber = rand() % 101;
	char getch = _getch();
	return 0;
}